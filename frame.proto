syntax = "proto3";

package BSB_Frame;

enum Encoding {
    PLAIN = 0;
    RUN_LENGTH = 1;
}

enum Screen {
    FRONT = 0;
    BACK = 1;
}

message Frame {
    Screen screen = 1;

    uint32 width = 2;
    uint32 height = 3;

    Encoding encoding = 4;

    bytes data = 5;
}
