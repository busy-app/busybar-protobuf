syntax = "proto3";

import "util/json.proto";

package BSB_Timer;

message Profile {
    string name = 1;
    BSB_Util.Json json = 2;
}

message Timer {
    BSB_Util.Json json = 1;
    repeated Profile profiles = 2;
}
